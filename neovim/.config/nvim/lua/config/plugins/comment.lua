require('Comment').setup({
    padding = true,

    toggler = {
        line = "gcc",
        block = "bgc"
    },

    mappings = {
        basic = true,
        extra = true,
        extended = true
    }
})
